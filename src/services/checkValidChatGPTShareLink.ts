export const checkValidChatGPTShareLink = (chatGPTShareLink: URL) => {
    // Todo: check chatGPTShareLink is a valid ChatGPT share link

    // 1. Check the link starts with https://chatgpt.com/share/


    // 2. Is there a particular format for the identifier? If there is, check the identifier follows that format


    // 3. Check that when the page is visited, it actually contains a chat (this may be the serialization thing)


    // Should return true or false, depending on whether it's a valid link or not
    return chatGPTShareLink;
}